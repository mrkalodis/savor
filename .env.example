# Savor Configuration
# Copy this file to .env and modify as needed

# Server port (default: 3000)
PORT=3000

# Data directory for SQLite database and images (default: ./data)
DATA_DIR=./data

# Node environment (default: production)
NODE_ENV=production
